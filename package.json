{
  "name": "cool-gpt",
  "version": "0.1.0",
  "private": true,
  "description": "Harness de chat IA avec boucle agentique (web, météo, Wikipédia, moteur Python) et rendu Markdown complet.",
  "scripts": {
    "dev": "concurrently -n serveur,web -c blue,magenta \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server run start",
    "install:all": "npm install && npm --prefix server install && npm --prefix web install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "gif-encoder": "^0.7.2",
    "gifenc": "^1.0.3",
    "playwright-core": "^1.62.1",
    "pngjs": "^7.0.0"
  }
}
